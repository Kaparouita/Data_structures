




Ask a. i)
flight Search(node L, int id)
{
    if (L == NULL)
        return NULL;
    struct flight *flight;
    flight = L->head;
    /*vres to year tou id*/
    while ((flight->id) < id && flight != NULL)
    {
        flight = flight->ynext;
    }
    /*des an yparxei to id*/
    while (flight->id >= id && flight != NULL) /*Ta id einai taksinomimena me auksousa?*/
    {
        if (flight->id == id)
            return flight;
        flight = flight->next;
    }
    /*kane return NULL an den yparxei*/
    return NULL;
}

ii)

void split(node L, node Before, node After)
{
    struct flight *flight, *tmp;
    flight = L->head;
    if (L->head == NULL) // IsEmptyList();
        return NULL;

    while (flight->year < 2 && flight != NULL) // 2015
    {
        tmp = flight;
        flight = flight->next;
    }
    Before->head = L->head;
    After->head = tmp->next;
    tmp->next = NULL;
}
